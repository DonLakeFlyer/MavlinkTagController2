# Runner configuration for GitHub Copilot agentic code review.
# https://gh.io/AA11tgch
on: workflow_dispatch

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    steps:
      - name: No setup required
        run: "true"
